{
  "name": "@prodeapp/subgraph",
  "version": "1.0.0",
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0"
  },
  "license": "MIT",
  "scripts": {
    "auth": "graph auth --product hosted-service $GRAPH_ACCESS_TOKEN",
    "build": "graph build",
    "codegen": "graph codegen --output-dir src/types/",
    "deploy": "graph deploy --product hosted-service prodeapp/prodeapp",
    "deploy:ads": "graph deploy --product hosted-service prodeapp/prodeapp-adds"
  }
}
